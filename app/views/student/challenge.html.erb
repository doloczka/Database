<%= link_to "Mapa świata", student_path(session[:user_id]) %>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
 <% i=0%>
  <tbody>
    <% @idgru.each do |stud| %>
      <tr>
        <td><%= i=1+i %></td>
        <td><%= stud.imie %></td>
        <td><%= stud.nazwisko %></td>
        <td><%= link_to "wybierz zadanie", student_chosechallenge_path(:idst => stud.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>