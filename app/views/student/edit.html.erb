<h1>Editing Student</h1>

<%= form_for @student do |f| %>
  <% if @student.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student.errors.count, "error") %> prohibited this grupy from being saved:</h2>

      <ul>
      <% @student.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
  <%=f.label :nralbumu %>
  <%=f.text_field :nralbumu%>
</div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show',  @student %> |
<%= link_to 'Back', grupy_path(@student.grupy_id) %>