<%= render "panel" %>
<%= link_to "Mapa świata", student_path(session[:user_id]) %>
<br>Wyzywasz <%= @wyzwany.imie %> <%= @wyzwany.nazwisko %> na pojedynek</br>
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
 <% i=0%>
  <tbody>
    <% @zadania.each do |zad| %>
      <tr>
        <td><%= i=1+i %></td>
        <td><%= zad.tresc %></td>
        <td><%= link_to "sprawdz go", student_challengeodp_path(:idst => @wyzwany.id, :zada => zad.id)%></td>
      </tr>
    <% end %>
  </tbody>
</table>